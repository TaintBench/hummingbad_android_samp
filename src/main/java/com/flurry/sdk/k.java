package com.flurry.sdk;

import java.util.HashSet;

final class k extends HashSet {
    k() {
        add(de.AC_NOTIFY_USER);
        add(de.AC_NEXT_FRAME);
        add(de.AC_CLOSE_AD);
        add(de.AC_VERIFY_URL);
    }
}
